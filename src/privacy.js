// privacy.js — /privacy route entry. No card, no three.js; just tokens, prose
// styles, and the shared chrome (theme toggle + auto-updating year).
import './tokens.css';
import './privacy.css';
import { initChrome } from './chrome.js';

initChrome();
